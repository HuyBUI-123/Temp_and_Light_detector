// Copy this file to "secrets.h" and fill in your own values.
// secrets.h is gitignored so your credentials never get committed.
#pragma once

// --- WiFi ---
#define WIFI_SSID     "your-wifi-name"
#define WIFI_PASSWORD "your-wifi-password"

// --- Backend ---
// Full URL of the readings endpoint on your home server.
// Use the server's LAN IP (reserved/static recommended) and port.
#define BACKEND_URL "http://192.168.1.50:8000/api/readings"

// Identifies this device in the payload / database.
#define DEVICE_ID "esp32-livingroom"
